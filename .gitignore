/node_modules
/.next
/out
.env
.env.local
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel

# scratch files from debugging
*.tmp.json

# the Date-Diary project happens to live in this folder; it is its own repo
/Date-Diary
