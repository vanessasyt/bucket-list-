# Postgres connection string. On Vercel, add a Postgres database in the
# Storage tab and this is provided automatically. It is the only variable
# the app needs: photos are stored in the database too.
DATABASE_URL=
